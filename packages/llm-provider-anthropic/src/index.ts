/**
 * @sketchmind/llm-provider-anthropic
 *
 * Anthropic adapter. Second provider, proves the abstraction holds.
 *
 * Public API only. Implementation belongs in src/internal/ and is not
 * importable from other packages (Volume 12).
 */

export const PACKAGE_NAME = "@sketchmind/llm-provider-anthropic";
export const PACKAGE_VERSION = "0.0.1";
