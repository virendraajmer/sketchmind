/**
 * SketchMind frontend shell.
 *
 * Phase 9 grows this into the real surface: prompt input, whiteboard canvas
 * mounting renderer-konva, playback controls, the agent trace panel, and the
 * diagram inspector.
 */
export default function Page() {
  return (
    <main>
      <h1>SketchMind</h1>
    </main>
  );
}
