# tools/

Internal developer tooling that is not published as a package: code generators,
schema generators, migration helpers, debugging utilities.

If a tool is used by CI or is a one-off automation, it belongs in `scripts/`.
